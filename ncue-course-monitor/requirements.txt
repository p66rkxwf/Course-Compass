requests==2.31.0
beautifulsoup4==4.12.2
# 直接相依：ncue_client 用 urllib3.util.Retry 做連線層重試。
# allowed_methods 是 1.26 才有的參數（2.0 移除了舊的 method_whitelist），
# requests 只要求 >=1.21.1,<3，不鎖的話 CI 可能解到會 TypeError 的版本。
urllib3==2.6.3
